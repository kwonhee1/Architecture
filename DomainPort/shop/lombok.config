config.stopBubbling = true
# @RequiredArgsConstructor 가 만드는 생성자 파라미터에 @Lazy 를 그대로 전달한다.
# product 도메인이 OrderDomainPort 를, order 도메인이 ProductDomainPort 를 서로 필요로 해
# 빈 생성 시점에 순환이 생기므로, 한쪽을 지연 주입하기 위해 사용한다.
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
